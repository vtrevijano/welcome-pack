/* import { getAllPokemons } from "./info.js";

const paintPokemon = pokemons => {
    const totalInfo = pokemons.map(
        (pokemon) =>
        `<li class="pokeList">
        <h1>${pokemon.name}</h1>
        <img src="${pokemon.image}" alt="${pokemon.name}"/>
        <button class="detail__button">Ver detalle</button>
        </li>`
    ).join('');
    totalPokemon.innerHTML = totalInfo
    window.addEventListener('load', getAllPokemons)
}

export {paintPokemon} */